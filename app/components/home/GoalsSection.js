import React, {Component} from 'react';
import {View, Text} from 'react-native';
import GoalItems from './GoalItem';

export default GoalSection = ({list}) => {
  return (
    <View>
      <Text>{}</Text>
    </View>
  );
};
